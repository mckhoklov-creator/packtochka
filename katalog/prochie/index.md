---
layout: default
title: "Прочие упаковочные материалы — аксессуары и расходники | ПакТочка Екатеринбург"
description: "Дополнительные упаковочные материалы и аксессуары. Узнайте наличие и закажите нужные позиции с доставкой по Екатеринбургу и России."
permalink: /katalog/prochie/
---

<h1>Прочие упаковочные материалы</h1>

{% assign items = site.data.products | where: "category", "prochie" %}
{% if items.size > 0 %}
<div class="grid">
  {% for p in items %}
  <div class="card">
    <a href="{{ '/katalog/prochie/' | append: p.slug | append: '/' | relative_url }}">
      <img src="{{ p.images | first | relative_url }}" alt="{{ p.name }}">
      <h3>{{ p.name }}</h3>
    </a>
    <p class="price">{{ p.price }} {{ p.unit }}</p>
    <p class="short">{{ p.short }}</p>
    <button class="btn btn-gradient mt-2 add-to-cart"
            data-sku="{{ p.slug }}"
            data-name="{{ p.name }}"
            data-price="{{ p.price }}">
      В корзину
    </button>
  </div>
  {% endfor %}
</div>
{% else %}
<p>Ассортимент этой категории формируется. Свяжитесь с нами, чтобы подобрать упаковочные аксессуары под ваши задачи.</p>
{% endif %}

<!-- 🔹 SEO-текст -->
<section class="seo-text">
  <p><strong>ПакТочка</strong> поставляет дополнительные упаковочные решения: плёнку, бумагу, рулонные материалы, расходники для маркировки и прочие позиции.</p>
  <p>Мы подберём товары по нужным параметрам, подготовим коммерческое предложение и доставим заказ по Екатеринбургу или отправим транспортной компанией по России.</p>
  <p>Если вы не нашли нужный материал, напишите нам — подберём индивидуальное решение и оперативно рассчитаем стоимость.</p>
</section>

<style>
  .seo-text { margin-top:32px; max-width:960px; color:#555; font-size:14px; line-height:1.6; }
  .seo-text p { margin:0 0 10px; }
</style>

